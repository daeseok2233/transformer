# === Core ===
torch            # PyTorch (CUDA/CPU 환경에 맞는 버전 설치)
numpy
nltk
konlpy           # 한국어 형태소 분석기 (Okt 사용)
# konlpy 사용 시 Java 필요: openjdk-11-jdk (Ubuntu/Debian), brew install openjdk@11 (macOS), conda install -c conda-forge openjdk=11 jpype1(window)

# === Optional: 데이터 처리 / 로더 ===
# 현재 코드에선 표준 라이브러리와 torch만 사용하지만,
# Pandas나 tqdm 등을 쓸 계획이 있다면 미리 추가 가능
# pandas
# tqdm
